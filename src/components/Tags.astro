---
import allTags from "~/data/allTags";
import Tag from "./Tag.astro";

const tags = allTags.map((tag) => tag);
---

<div class="flex items-center space-x-4">
  <Tag href="/blog" name="todos" />
  {tags.map((tag) => <Tag name={tag} />)}
</div>
